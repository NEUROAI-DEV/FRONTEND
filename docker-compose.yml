services:
  app:
    build:
      context: .
      target: development
    container_name: neuroai-frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
